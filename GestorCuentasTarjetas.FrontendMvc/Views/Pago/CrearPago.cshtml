@model GestorCuentasTarjetas.FrontendMvc.Models.NuevoPago

@{
    ViewData["Title"] = "CrearPago";

}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h3 class="card-title">Registro Pago de TC</h3>
                </div>
                <div class="card-body">
                    
                    <form asp-action="CrearPago">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="form-group">
                            <label asp-for="Monto" class="control-label">Monto:</label>
                            <input asp-for="Monto" class="form-control" />
                            <span asp-validation-for="Monto" class="text-danger"></span>
                        </div>
                        <br />
                        <div class="form-group">
                            <input type="submit" value="Guardar" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
                <style>
                .card-footer.text-right {
                text-align: right;
                }
                </style>
                <div class="card-footer text-muted text-right">
                    <a asp-controller="Tarjeta" asp-action="Index" class="btn btn-danger">Cancelar</a>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
